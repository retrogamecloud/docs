---
title: 1.1 Inicio Rápido
description: Despliega Retro Game Hub en AWS en menos de 30 minutos
icon: rocket
---

<img
  className="block dark:hidden"
  src="/images/hero-light.png"
  alt="Retro Game Hub Hero"
/>
<img
  className="hidden dark:block"
  src="/images/hero-dark.png"
  alt="Retro Game Hub Hero"
/>

## ¿Qué es Retro Game Hub?

Una plataforma completa para jugar **juegos clásicos de DOS** en la nube, construida con:

<CardGroup cols={3}>
  <Card title="Kubernetes" icon="dharmachakra">
    Orquestación en AWS EKS
  </Card>
  <Card title="Microservicios" icon="cubes">
    5 servicios independientes
  </Card>
  <Card title="Terraform" icon="code">
    Infraestructura como código
  </Card>
</CardGroup>

## Requisitos Previos

<AccordionGroup>
  <Accordion icon="aws" title="Cuenta de AWS">
    - Cuenta AWS activa con permisos de administrador
    - AWS CLI v2 configurado con tus credenciales
    - Región recomendada: `eu-west-1` (Irlanda)

    <Tip>
      **Costos estimados**: ~$135-160/mes (EKS $72 + instancias + RDS + NAT Gateway)
    </Tip>
  </Accordion>

  <Accordion icon="terminal" title="Herramientas Locales">
    Instala las siguientes herramientas en tu máquina:

    ```bash
    # Terraform
    wget https://releases.hashicorp.com/terraform/1.6.0/terraform_1.6.0_linux_amd64.zip
    unzip terraform_1.6.0_linux_amd64.zip
    sudo mv terraform /usr/local/bin/

    # kubectl
    curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
    sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

    # AWS CLI
    curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
    unzip awscliv2.zip
    sudo ./aws/install
    ```

  </Accordion>

  <Accordion icon="globe" title="Dominio (Opcional)">
    Si quieres usar tu propio dominio:
    - Dominio registrado (ej: Namecheap, GoDaddy)
    - Acceso para cambiar nameservers

    <Note>
      El proyecto usa Route53 para gestión de DNS. Si no tienes dominio, se creará uno automáticamente usando xip.io
    </Note>
  </Accordion>
</AccordionGroup>

## Despliegue en 3 Pasos

### Paso 1: Clonar el Repositorio

```bash
git clone https://github.com/tu-usuario/retro-game-hub.git
cd retro-game-hub/infrastructure

```

### Paso 2: Configurar Variables

```bash

# Copiar archivo de configuración
cp terraform.tfvars.example terraform.tfvars

# Editar con tus valores
nano terraform.tfvars

```

<CodeGroup>

```hcl terraform.tfvars

# Configuración básica
region = "eu-west-1"
project_name = "retro-game-hub"
environment = "prod"

# Dominio (opcional)
domain_name = "tudominio.com"  # o déjalo vacío

# Base de datos
db_password = "tu-password-seguro"

# Configuración EKS
cluster_name = "retro-game-hub-cluster"
node_group_instance_types = ["t3.medium"]
node_group_desired_size = 2
node_group_max_size = 4
node_group_min_size = 1

```

</CodeGroup>

### Paso 3: Desplegar Infraestructura

```bash

# Inicializar Terraform
terraform init

# Planificar despliegue
terraform plan

# Aplicar cambios
terraform apply -auto-approve

```

<Warning>
  El despliegue toma aproximadamente **20-25 minutos**. No interrumpas el proceso.
</Warning>

## Configurar kubectl

Una vez completado el despliegue:

```bash

# Configurar acceso al cluster
aws eks update-kubeconfig --region eu-west-1 --name retro-game-hub-cluster

# Verificar conexión
kubectl get nodes

```

## Verificar Despliegue

```bash

# Ver todos los pods
kubectl get pods -A

# Ver servicios
kubectl get svc -n retro-game-hub

# Obtener URL de acceso
kubectl get ingress -n retro-game-hub

```

<Tip>
  **¡Listo!** Tu plataforma Retro Game Hub está funcionando. Accede usando la URL mostrada en el ingress.
</Tip>

## Próximos Pasos

<CardGroup cols={2}>
  <Card title="Configuración Avanzada" icon="gear" href="/configuration">
    Personaliza tu instalación
  </Card>
  <Card title="Añadir Juegos" icon="gamepad" href="/games/adding-games">
    Subir tus ROMs favoritas
  </Card>
  <Card title="Monitoreo" icon="chart-line" href="/monitoring">
    Dashboard de métricas
  </Card>
  <Card title="Troubleshooting" icon="wrench" href="/troubleshooting">
    Resolver problemas comunes
  </Card>
</CardGroup>