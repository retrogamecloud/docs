---
title: 7.2. GitHub Actions
description: Workflows automatizados de CI/CD con tests end-to-end y promoción multi-entorno
icon: github
---

## Workflows Implementados

RetroGame Cloud utiliza GitHub Actions para automatizar completamente el ciclo de vida del código desde commit hasta producción, incluyendo validación mediante tests end-to-end, análisis de calidad y promoción controlada entre ambientes.

<CardGroup cols={3}>
  <Card title="docker-publish-and-update-k8s.yml" icon="docker">
    **Pipeline principal CI/CD**

    Build, test E2E, push Docker y actualizar manifiestos K8s

    *En todos los servicios*
  </Card>

  <Card title="ci.yml" icon="check">
    **Validación de manifiestos**

    Tests y validación de YAMLs de Kubernetes

    *Repo: kubernetes/*
  </Card>

  <Card title="e2e-tests.yml" icon="play">
    **Tests End-to-End**

    Validación de flujos completos de usuario

    *Todos los repos de servicios*
  </Card>

  <Card title="multi-env-deploy.yml" icon="layers">
    **Despliegue multi-entorno**

    Promoción automática dev→staging→prod

    *Con gates de aprobación*
  </Card>

  <Card title="quality-gate.yml" icon="shield">
    **Gates de calidad**

    SonarQube, Trivy, tests de cobertura

    *Bloqueo automático si falla*
  </Card>

  <Card title="rollback.yml" icon="arrow-rotate-left">
    **Rollback automático**

    Detección de fallos y rollback

    *Monitoreo post-deploy*
  </Card>

  <Card title="update-docs-with-claude.yml" icon="robot">
    **Actualización automática de docs**

    Claude AI actualiza la wiki/documentación

    *Repo: docs/*
  </Card>

  <Card title="deploy-mintlify-docs.yml" icon="book">
    **Despliegue de documentación**

    Deploy automático a Mintlify

    *Repo: docs/*
  </Card>

  <Card title="terraform-plan.yml" icon="code">
    **Validación de Terraform**

    Plan y validación de infraestructura

    *Repo: infrastructure/*
  </Card>

  <Card title="terraform-apply.yml" icon="rocket">
    **Despliegue de infraestructura**

    Apply de Terraform en producción

    *Repo: infrastructure/*
  </Card>
</CardGroup>

---

## Pipeline Principal Completo

Este es el workflow completo que se ejecuta en cada repositorio de servicio con todas las etapas de calidad y seguridad.

### Estructura del Pipeline

<Steps>
  <Step title="Lint & Format">
    Validación de código y formato estándar
  </Step>
  <Step title="Unit Tests">
    Tests unitarios con cobertura mínima 80%
  </Step>
  <Step title="Build Docker">
    Construcción de imagen optimizada multi-stage
  </Step>
  <Step title="Security Scan">
    Análisis de vulnerabilidades con Trivy
  </Step>
  <Step title="Quality Gate">
    SonarQube analysis y quality gates
  </Step>
  <Step title="Push to ECR">
    Subida a Amazon Elastic Container Registry
  </Step>
  <Step title="Update K8s Manifests">
    Actualización de manifiestos GitOps
  </Step>
  <Step title="ArgoCD Sync">
    Trigger de sincronización automática
  </Step>
</Steps>

### Pipeline Completo Anotado

<CodeGroup>

```yaml Configuración Principal
name: CI/CD Pipeline - Build, Test, Scan & Deploy

on:
  push:
    branches: [main, develop]
    paths-ignore: ['**.md', '.gitignore', 'docs/**']
  pull_request:
    branches: [main, develop]
    paths-ignore: ['**.md', '.gitignore', 'docs/**']

env:
  AWS_REGION: us-east-1
  ECR_REPOSITORY: retrogame-${{ github.event.repository.name }}
  SONAR_PROJECT_KEY: retrogame-${{ github.event.repository.name }}
  ARGOCD_SERVER: argocd.retrogame.cloud
```

```yaml Jobs - Lint & Test
jobs:
  lint-and-format:
    name: 🔍 Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run ESLint
        run: npm run lint
      
      - name: Check Prettier formatting
        run: npm run format:check
      
      - name: TypeScript check
        run: npm run type-check

  unit-tests:
    name: 🧪 Unit Tests
    runs-on: ubuntu-latest
    needs: lint-and-format
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run unit tests
        run: npm run test:coverage
      
      - name: Check coverage threshold
        run: |
          COVERAGE=$(cat coverage/coverage-summary.json | jq '.total.lines.pct')
          if (( $(echo "$COVERAGE < 80" | bc -l) )); then
            echo "❌ Coverage $COVERAGE% is below 80% threshold"
            exit 1
          fi
          echo "✅ Coverage $COVERAGE% meets threshold"
      
      - name: Upload coverage to SonarQube
        uses: sonarqube-quality-gate-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
```

```yaml Build & Security
  build-and-scan:
    name: 🐳 Build & Security Scan
    runs-on: ubuntu-latest
    needs: unit-tests
    outputs:
      image-digest: ${{ steps.build.outputs.digest }}
      image-tag: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@v4
      
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}
      
      - name: Login to Amazon ECR
        uses: aws-actions/amazon-ecr-login@v2
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ secrets.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}
      
      - name: Build Docker image
        id: build
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Run Trivy vulnerability scanner
        uses: aquasecurity/trivy-action@master
        with:
          image-ref: ${{ steps.meta.outputs.tags }}
          format: 'sarif'
          output: 'trivy-results.sarif'
          exit-code: '1'
          severity: 'CRITICAL,HIGH'
      
      - name: Upload Trivy scan results
        uses: github/codeql-action/upload-sarif@v3
        if: always()
        with:
          sarif_file: 'trivy-results.sarif'
```

```yaml Quality Gate & Deploy
  quality-gate:
    name: 🛡️ Quality Gate
    runs-on: ubuntu-latest
    needs: build-and-scan
    steps:
      - uses: actions/checkout@v4
      
      - name: SonarQube Analysis
        uses: sonarqube-quality-gate-action@master
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
        with:
          projectKey: ${{ env.SONAR_PROJECT_KEY }}
          qualityGateWait: true
      
      - name: Quality Gate Status
        run: |
          if [[ "${{ steps.sonarqube-quality-gate-check.outputs.quality-gate-status }}" == "FAILED" ]]; then
            echo "❌ SonarQube Quality Gate failed"
            exit 1
          fi
          echo "✅ SonarQube Quality Gate passed"

  update-manifests:
    name: 📝 Update K8s Manifests
    runs-on: ubuntu-latest
    needs: [build-and-scan, quality-gate]
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Checkout kubernetes repo
        uses: actions/checkout@v4
        with:
          repository: retrogame-cloud/kubernetes
          token: ${{ secrets.GITOPS_TOKEN }}
          path: k8s-manifests
      
      - name: Update image tag
        run: |
          cd k8s-manifests
          IMAGE_TAG=$(echo "${{ needs.build-and-scan.outputs.image-tag }}" | cut -d':' -f2)
          
          # Update development environment first
          yq eval ".spec.template.spec.containers[0].image = \"${{ secrets.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:${IMAGE_TAG}\"" \
            -i apps/${{ github.event.repository.name }}/overlays/dev/deployment.yaml
          
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git add .
          git commit -m "🚀 Update ${{ github.event.repository.name }} to ${IMAGE_TAG}"
          git push

  trigger-argocd:
    name: 🚀 Trigger ArgoCD Sync
    runs-on: ubuntu-latest
    needs: update-manifests
    steps:
      - name: ArgoCD CLI Login & Sync
        run: |
          curl -sSL -o argocd-linux-amd64 https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
          
          argocd login ${{ env.ARGOCD_SERVER }} \
            --username ${{ secrets.ARGOCD_USERNAME }} \
            --password ${{ secrets.ARGOCD_PASSWORD }} \
            --insecure
          
          argocd app sync retrogame-${{ github.event.repository.name }}-dev
          argocd app wait retrogame-${{ github.event.repository.name }}-dev --timeout 300

  notify:
    name: 📱 Notify Results
    runs-on: ubuntu-latest
    needs: [trigger-argocd]
    if: always()
    steps:
      - name: Notify Slack Success
        if: needs.trigger-argocd.result == 'success'
        uses: 8398a7/action-slack@v3
        with:
          status: success
          channel: '#deployments'
          message: |
            ✅ *${{ github.event.repository.name }}* deployed successfully to DEV
            
            📦 Image: `${{ needs.build-and-scan.outputs.image-tag }}`
            🔗 Commit: <${{ github.event.head_commit.url }}|${{ github.sha }}>
            👤 Author: ${{ github.event.head_commit.author.name }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      
      - name: Notify Slack Failure
        if: failure()
        uses: 8398a7/action-slack@v3
        with:
          status: failure
          channel: '#alerts'
          message: |
            ❌ *${{ github.event.repository.name }}* deployment failed
            
            🔗 Workflow: <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|View Details>
            👤 Author: ${{ github.event.head_commit.author.name }}
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
```

</CodeGroup>

---

## Pipeline Multi-Entorno

Estrategia de promoción controlada entre ambientes con gates de aprobación.

### GitFlow Strategy

<CodeGroup>

```yaml Branching Strategy
# Configuración de branches y promociones
branches:
  develop:    # → DEV environment (automático)
    - Merge automático tras PR approval
    - Deploy automático a dev
    - Tests E2E en ambiente dev
  
  release/*:  # → STAGING environment (automático)
    - Creación manual desde develop
    - Deploy automático a staging
    - Tests de regresión completos
  
  main:       # → PRODUCTION (manual approval)
    - Merge desde release branch
    - Requiere approval de 2 maintainers
    - Deploy con approval gate
    - Monitoreo post-deploy automático
```

```yaml Multi-Environment Pipeline
name: Multi-Environment Deployment

on:
  push:
    branches: [develop, 'release/*', main]

jobs:
  determine-environment:
    runs-on: ubuntu-latest
    outputs:
      environment: ${{ steps.env.outputs.environment }}
      requires-approval: ${{ steps.env.outputs.requires-approval }}
    steps:
      - id: env
        run: |
          if [[ "${{ github.ref }}" == "refs/heads/develop" ]]; then
            echo "environment=dev" >> $GITHUB_OUTPUT
            echo "requires-approval=false" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            echo "environment=staging" >> $GITHUB_OUTPUT
            echo "requires-approval=false" >> $GITHUB_OUTPUT
          elif [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "environment=prod" >> $GITHUB_OUTPUT
            echo "requires-approval=true" >> $GITHUB_OUTPUT
          fi

  deploy:
    runs-on: ubuntu-latest
    needs: determine-environment
    environment: 
      name: ${{ needs.determine-environment.outputs.environment }}
      url: https://${{ needs.determine-environment.outputs.environment }}.retrogame.cloud
    steps:
      - name: Deploy to ${{ needs.determine-environment.outputs.environment }}
        run: |
          echo "🚀 Deploying to ${{ needs.determine-environment.outputs.environment }}"
          # Deployment logic here
```

</CodeGroup>

### Approval Gates para Producción

<CodeGroup>

```yaml Production Approval
environment:
  name: prod
  protection_rules:
    - type: required_reviewers
      reviewers:
        - retrogame-cloud/maintainers
        - retrogame-cloud/devops-team
    - type: wait_timer
```