name: Notify Docs - Kubernetes

on:
  push:
    branches:
      - main
      - master
    paths:
      - '**/*.yml'
      - '**/*.yaml'
      - 'deployments/**/*'
      - 'services/**/*'
      - 'configmaps/**/*'

jobs:
  notify-docs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Trigger docs repository update
        run: |
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.PAT_TOKEN }}" \
            https://api.github.com/repos/retrogamecloud/docs/dispatches \
            -d '{"event_type":"docs-update","client_payload":{"repository":"kubernetes","ref":"${{ github.ref }}","sha":"${{ github.sha }}"}}'
            
      - name: Notification sent
        run: echo "📚 Kubernetes docs sync triggered"
