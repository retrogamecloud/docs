name: Sync Documentation from Backend

on:
  # Trigger manual
  workflow_dispatch:
  
  # Trigger desde los repos usando repository_dispatch
  repository_dispatch:
    types: 
      - backend-updated
      - frontend-updated
      - infrastructure-updated
      - kong-updated
      - kubernetes-updated
  
  # O programado cada 6 horas
  schedule:
    - cron: '0 */6 * * *'

jobs:
  sync-docs:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout docs repo
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          
      - name: Checkout backend repo
        uses: actions/checkout@v4
        with:
          repository: retrogamecloud/backend
          path: .repos/backend
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 2
          
      - name: Checkout frontend repo
        uses: actions/checkout@v4
        with:
          repository: retrogamecloud/frontend
          path: .repos/frontend
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 2
          
      - name: Checkout infrastructure repo
        uses: actions/checkout@v4
        with:
          repository: retrogamecloud/infrastructure
          path: .repos/infrastructure
          token: ${{ secrets.PAT_TOKEN }}
          
      - name: Checkout kong repo
        uses: actions/checkout@v4
        with:
          repository: retrogamecloud/kong
          path: .repos/kong
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 2
          
      - name: Checkout kubernetes repo
        uses: actions/checkout@v4
        with:
          repository: retrogamecloud/kubernetes
          path: .repos/kubernetes
          token: ${{ secrets.PAT_TOKEN }}
          
      - name: Install documentation tools
        run: |
          npm install -g jsdoc jsdoc-to-markdown
          
      - name: Generate API documentation
        run: |
          mkdir -p temp-docs
          
          echo "📚 Generating Backend API Documentation..."
          # Backend services
          for service in auth-service game-catalog-service score-service ranking-service user-service; do
            if [ -d "/tmp/repos/backend/src/${service}" ]; then
              echo "  → ${service}"
              jsdoc2md "/tmp/repos/backend/src/${service}/**/*.js" > "temp-docs/${service}-api.md" 2>/dev/null || echo "    ⚠️  No JSDoc found"
            fi
          done
          
          # Si el backend tiene estructura diferente
          if [ -f "/tmp/repos/backend/index.js" ]; then
            echo "  → backend-main"
            jsdoc2md "/tmp/repos/backend/**/*.js" --exclude "node_modules/**" > "temp-docs/backend-main-api.md" 2>/dev/null || echo "    ⚠️  No JSDoc found"
          fi
          
          echo "🎨 Generating Frontend Documentation..."
          # Frontend
          if [ -d "/tmp/repos/frontend" ]; then
            echo "  → frontend-client"
            jsdoc2md "/tmp/repos/frontend/**/*.js" --exclude "node_modules/**" --exclude "jsdos/**" > "temp-docs/frontend-api.md" 2>/dev/null || echo "    ⚠️  No JSDoc found"
          fi
          
          echo "🏗️ Generating Infrastructure Documentation..."
          # Infrastructure (Terraform)
          if [ -d "/tmp/repos/infrastructure" ]; then
            echo "  → terraform-modules"
            # Extraer descripciones de variables.tf y outputs.tf
            find /tmp/repos/infrastructure -name "*.tf" -type f | while read tf_file; do
              service_name=$(basename $(dirname "$tf_file"))
              echo "## Infrastructure: ${service_name}" >> "temp-docs/infrastructure-docs.md"
              grep -A 3 'variable\|output\|resource' "$tf_file" >> "temp-docs/infrastructure-docs.md" 2>/dev/null || true
            done
          fi
          
          echo "🔀 Generating Kong Configuration Documentation..."
          # Kong (YAML configs)
          if [ -d "/tmp/repos/kong" ]; then
            echo "  → kong-gateway"
            {
              echo "# Kong Gateway Configuration"
              echo ""
              find /tmp/repos/kong -name "*.yml" -o -name "*.yaml" | while read yml_file; do
                echo "## $(basename $yml_file)"
                echo "\`\`\`yaml"
                cat "$yml_file"
                echo "\`\`\`"
                echo ""
              done
            } > "temp-docs/kong-config.md"
          fi
          
          echo "☸️ Generating Kubernetes Documentation..."
          # Kubernetes (manifests)
          if [ -d "/tmp/repos/kubernetes" ]; then
            echo "  → k8s-manifests"
            {
              echo "# Kubernetes Manifests"
              echo ""
              find /tmp/repos/kubernetes -name "*.yml" -o -name "*.yaml" | while read yml_file; do
                echo "## $(basename $yml_file)"
                echo "\`\`\`yaml"
                cat "$yml_file"
                echo "\`\`\`"
                echo ""
              done
            } > "temp-docs/kubernetes-manifests.md"
          fi
          
          echo "✅ Documentation generation complete!"
          ls -lh temp-docs/
          
      - name: Transform to MDX format
        run: |
          node .github/scripts/transform-to-mdx.js
      
      # AI-powered documentation analysis with Claude
      - name: Setup Python for Claude
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      - name: Install Anthropic SDK
        run: |
          pip install anthropic
      
      - name: Analyze Changes with Claude
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          echo "🤖 Analizando cambios con Claude AI..."
          
          # Analizar cada repositorio
          for repo in backend frontend kong; do
            if [ -d "/tmp/repos/$repo" ]; then
              echo "📂 Analizando $repo..."
              python .github/scripts/claude-doc-generator.py "$repo" "/tmp/repos/$repo" || true
            fi
          done
          
          echo "✅ Análisis con Claude completado"
      
      - name: Cleanup temporary directories
        run: |
          echo "🧹 Cleaning up temporary directories..."
          rm -rf .repos temp-docs
          echo "✅ Cleanup complete"
          
      - name: Check for changes
        id: check_changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --staged --quiet; then
            echo "has_changes=false" >> $GITHUB_OUTPUT
          else
            echo "has_changes=true" >> $GITHUB_OUTPUT
          fi
          
      - name: Commit and push changes
        if: steps.check_changes.outputs.has_changes == 'true'
        run: |
          git commit -m "docs: auto-sync API documentation from backend

          - Generated from backend repository
          - Updated service endpoints and schemas
          - Automated by GitHub Actions"
          git pull --rebase
          git push
